#version 300 es
uniform sampler2D texture;
in vec2 TexCoord;
out vec4 fragColor;
void main() {
    vec4 texColor = texture(texture, TexCoord);
    if (texColor.a < 0.1) {
        discard;
    }
    fragColor = texColor;
}