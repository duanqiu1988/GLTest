varying vec2 TexCoord;
varying vec3 outColor;
uniform sampler2D outTexture;
uniform sampler2D outTexture2;
void main() {
    gl_FragColor = mix(texture2D(outTexture, TexCoord), texture2D(outTexture2, TexCoord), 0.2) * vec4(outColor, 1);
    }