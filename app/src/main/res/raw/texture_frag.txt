#version 300 es
in vec2 TexCoord;
in vec3 outColor;
uniform sampler2D outTexture;
uniform sampler2D outTexture2;
uniform float mix;
out vec4 fragColor;
void main() {
    fragColor = mix(texture(outTexture, TexCoord), texture(outTexture2, TexCoord), mix) * vec4(outColor, 1);
    }